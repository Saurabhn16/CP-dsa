#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long t;
    cin >> t;
    while (t--)
    {
        long long q;
        cin >> n >
            int a[n];
        int sum = 0;

        for (int i = 1; i <= n; i++)
        {
            cin >> a[i];
            sum += a[i];
        }
    }
    return 0;
}