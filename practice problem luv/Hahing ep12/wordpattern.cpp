class Solution
{
public:
    bool wordPattern(string pattern, string s)
    {
        int l = pattern.size();
        int hsh[300];
        for (int i = 0; i < l; i++)
        {
            hsh[pattern[i]]++;
        }
        for (int i = 0; i < n; i++)
        {
            if (s[i] == ' ')
            {
            }
        }
    }
};