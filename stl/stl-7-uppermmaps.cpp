  #include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    // int a[n];
    set<int> a;
    map<int,int>m;
    for (int i = 0; i < n; ++i)
    {
        s.insert(rand());
    }
    for (int i = 0; i < n; i++)
    {
        auto it= lower_bound(s.begin(),s.end(  ),rand())
    }
    // cout << endl;
}